## 畫面佈局與 UI 概念

畫面可以採取 **「儀表板式 (Dashboard)」** 的配置，讓左右兩側的操作與回饋保持即時同步。

### 左側：動態繪圖區 (The Canvas)
這是視覺化的核心，重點在於**對比**。
* **底色圖層**：以淺灰色或虛線顯示原本的函數（例如 $\sin x$ 的完整曲線）。
* **動態圖層**：以鮮明色彩（如鮮橘色或科技藍）繪製當前的泰勒多項式近似曲線。
* **誤差著色 (Error Heatmap)**：在兩條曲線之間填充淡淡的陰影。當階數增加、曲線越貼合時，陰影面積會迅速縮小，這能給使用者極強的視覺回饋。
* **展開點標記**：用一個明顯的點標示出 $x = a$ 的位置，讓使用者看到所有曲線都是從這一點「生長」出來的。

### 右側：控制與互動面板 (The Control Center)
這裡負責引導使用者的邏輯思維。
* **公式切換器**：頂部提供下拉選單或圖示選單，可切換 $e^x$, $\cos x$, $\ln(1+x)$ 等經典函數。
* **階數控制 (The Order Stepper)**：
    * 這不只是個拉桿，建議設計成 **「分段式按鈕」**（Step 0, Step 1, Step 2...）。
    * 每增加一個階段，下方自動同步顯示當前產生的多項式片段。
* **動態公式顯示 (Live Formula)**：
    * 顯示完整的 $\sum$ 公式，並將當前正在計算的那一項用高亮顏色標出。
    * 例如：$P(x) = 1 + x + \frac{x^2}{2!} + \mathbf{\color{orange}{\frac{x^3}{3!}}}$
* **數值監測 (Numerical Insights)**：顯示一個簡單的數值表，對比特定 $x$ 點下的「真實值」與「近似值」，並算出當前的誤差率（%）。

---

## 視覺互動細節 (UX Features)

* **生長動畫 (Growth Animation)**：當使用者點選「下一步（Next Order）」時，曲線不應該直接跳轉，而是像「橡皮筋」一樣緩慢地從舊的形狀變形成新的形狀。
* **局部放大 (Zoom-in at Origin)**：提供一個功能，可以一鍵縮放到展開點附近，觀察即使是低階多項式，在極小範圍內也能完美重合的現象。
* **可拖移的中心點**：允許使用者用滑鼠拖動展開點（中心點 $a$），看著整條近似曲線隨著中心點位置改變而即時重新計算、扭轉，這會非常有說服力。



這個設計能讓使用者不僅是「看」公式，而是能「感覺」到多項式是如何一項一項地把複雜的波動給「拼湊」出來的。你覺得這樣的佈局符合你心中的畫面嗎？
